rootProject.name = "layered-encryption-protocol"

pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

include(":lep")
include(":playground")
include(":fuzz")
